package com.example.calendar.ui.viewmodel

enum class EnumTaskViewMode {
    LIST,         // O modo atual com TaskItem detalhado
    COMPACT_LIST, // Uma lista mais compacta (ex: apenas título)
    GRID          // Uma visualização em grade (cards lado a lado)
}